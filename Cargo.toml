[package]
name = "my-todo"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.8.6", features = ["sqlite", "runtime-tokio"] }

clap = "4.5.60"

anyhow = "1.0"

